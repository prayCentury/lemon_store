

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lemon_store?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
logging:
  level:
    com.pray.example.store.codeGenerator: debug
server:
  servlet:
    context-path: /api
